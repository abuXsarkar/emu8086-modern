# @emu8086/web

Web IDE for `emu8086-modern` — React 18 + TypeScript + Vite, with Monaco arriving in M3.

```bash
pnpm install
pnpm dev   # http://localhost:5173
```

Status: **M0 placeholder shell**. Editor, debugger, and panels arrive in M3.
