# modern8051-wasm-api

wasm-bindgen surface the modern8051 web IDE imports. Will mirror `modern8085-wasm-api`'s `Emulator` class (load / step / run / state / mem / poke / drain_io_log) once the executor lands.

Status: **skeleton** — just a `version()` probe so the JS side can verify wasm loaded.
