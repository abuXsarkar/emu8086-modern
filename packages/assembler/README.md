# emu8086-assembler

Two-pass assembler with `emu8086` and `nasm` dialects.

See [`../../docs/emu8086-compatibility.md`](../../docs/emu8086-compatibility.md) for the full dialect specification.

Status: **skeleton (M0)**. Implementation lands in M2.
