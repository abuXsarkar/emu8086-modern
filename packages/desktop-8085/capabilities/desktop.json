{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-only",
  "description": "Permissions only available on the desktop build of modern8085. Mirrors the modern8086-desktop split so the same gating logic applies (the updater plugin is upstream-gated on cfg(desktop) and doesn't exist when cross-compiling for Android / iOS).",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default"]
}
